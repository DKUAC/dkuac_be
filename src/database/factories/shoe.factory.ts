// import { ShoeModel } from 'src/rent/entities/shoe.entity';
// import { setSeederFactory } from 'typeorm-extension';

// const sizes = [230, 235, 240, 245, 250, 260, 270, 280];

// export default setSeederFactory(ShoeModel, (faker) => {
//   const shoe = new ShoeModel();

//   shoe.size = faker.random.(sizes);
//   shoe.count = faker.datatype.number({ min: 1, max: 10 });
// });
